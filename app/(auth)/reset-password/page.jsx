import ResetPasswordContent from "@/components/auth/password/ResetPasswordContent";
import React, { Suspense } from "react";

const ResetPasswordPage = () => {
  
  return (
    <Suspense>
      <ResetPasswordContent/>
    </Suspense>
  );
};

export default ResetPasswordPage;