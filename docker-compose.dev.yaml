services:
  dev-app:
    build: 
      context: .
      dockerfile: dockerfiles/dev.dockerFile
    container_name: auth-backend
    env_file:
      - .env
    # volumes:
    #   - .:/app     # Local repo(at path specfied) ko host machine ki (specified path) par dal deta hai
    ports:          
      - "3000:3000"   #👉 Host ↔ Container
    networks:
      - nextjs-app    #👉 Container ↔ Container

networks:
  nextjs-app:
    
    